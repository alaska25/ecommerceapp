import { Card, Button, CardGroup} from "react-bootstrap";

export default function SubFooter(){
	return(

	    	<div bg="dark" className="container-fluid">
	    		<div variant="dark" className="p-3 container text-center">
      				
	          	</div>
	    	</div>
		)
}